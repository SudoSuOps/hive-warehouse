{
  "name": "hive-warehouse",
  "version": "1.0.0",
  "description": "Honey Warehouse — Intelligence supply chain for Swarm & Bee",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create honey-db",
    "db:migrate": "wrangler d1 execute honey-db --file=migrations/001_schema.sql",
    "db:migrate:local": "wrangler d1 execute honey-db --local --file=migrations/001_schema.sql"
  },
  "author": "Swarm & Bee LLC",
  "license": "UNLICENSED",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
